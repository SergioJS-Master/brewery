/* eslint-disable object-curly-newline */
import styles from './CartItem.module.css'

export function CartItem({ id, size, name, picture, discription, discount, price }) {
  console.log(id, size, name, picture, discription, discount, price)
  return <div className={styles.wr}>asd</div>
}
